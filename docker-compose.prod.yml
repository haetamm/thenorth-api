services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: thenorth-app-prod
    env_file:
      - .env.prod
    ports:
      - "8000:8000"
    restart: unless-stopped
